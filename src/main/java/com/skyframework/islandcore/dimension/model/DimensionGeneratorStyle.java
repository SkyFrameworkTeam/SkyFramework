package com.skyframework.islandcore.dimension.model;

// Purely a declared intent for now: each style will be translated into a concrete ChunkGenerator
// configuration when Fantasy is integrated (Sprint 16). No generator behavior lives here yet.
public enum DimensionGeneratorStyle {
	OVERWORLD_LIKE,
	NETHER_LIKE,
	END_LIKE,
	VOID_FLAT
}
